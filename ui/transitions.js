S5.transitions.Basic = {
  defaultAction: true,
  name: 'basic',
  transitToSlide: function(i, s5) {
  }
}

S5.transitions.Dissolve = {
  defaultAction: true,
  name: 'dissolve',
  transitToSlide: function(i, s5) {
  }
}

S5.transitions.Push = {
  defaultAction: true,
  name: 'push',
  transitToSlide: function(i, s5) {
  }
}

