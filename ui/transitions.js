S5.transitions.Basic = {
  defaultAction: true,
  transitToSlide: function(i, s5) {
  }
}

S5.transitions.Dissolve = {
  defaultAction: true,
  transitToSlide: function(i, s5) {
  }
}

S5.transitions.Push = {
  defaultAction: true,
  transitToSlide: function(i, s5) {
  }
}

